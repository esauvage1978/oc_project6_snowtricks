<article class="card">
    <a href="{{ path('trick_show', {'slug': trick.slug}) }}">
        <img src="{{ absolute_url(asset('/img/trick_default.jpg')) }}" alt=""
             class="card-img-top">
    </a>
    <div class="card-body">
        <h4>
            <a href="{{ path('trick_show', {'slug': trick.slug}) }}">
                {{ trick.name }}
            </a>
        </h4>
    </div>


</article>